# compose/secrets.template
# -------------------------
# Copy to compose/secrets.env and set real values before starting any service.
#   cp compose/secrets.template compose/secrets.env
#
# Or let Make generate a random password automatically:
#   make secrets-init
#
# DO NOT commit secrets.env to version control — it is gitignored.

# Password for the TimescaleDB 'metrics' user.
# Used by TimescaleDB, Vector, and Grafana in the monitoring stack.
METRICS_DB_PASSWORD=